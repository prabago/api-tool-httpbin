environments:
  - name: local
    endpoints:
      production:
        url: https://eu.httpbin.org
        config:
        retryTimeOut: $RETRY       
      sandbox:         
      url: https://eu.httpbin.org
      config:
        retryTimeOut: $RETRY       

deploy:
  import:
    update: true
    preserveProvider: true
